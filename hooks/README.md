# Hooks

Canonical hook definitions live in [`plugins/mantle-forge/hooks/`](../plugins/mantle-forge/hooks/).

The compatible plugin bundle includes a `SessionStart` hook that verifies `MANTLE_FORGE_ROOT` is configured after install.
