# Templates

| Template | Purpose |
|----------|---------|
| `hardhat-mantle-starter/` | Minimal Lock demo + Mantle Sepolia config |
| `token-vault/` | Flagship ERC-20 vault (4 tests, deploy script, gas reporter) |

Scaffold:

```bash
npx mantle-scaffold token-vault ./my-vault
```
